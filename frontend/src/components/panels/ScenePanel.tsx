import { Box, Typography } from '@mui/material';

export function ScenePanel() {
  return (
    <Box sx={{ p: 2, width: 320 }}>
      <Typography variant="subtitle1">Scene</Typography>
    </Box>
  );
}

export default ScenePanel;
